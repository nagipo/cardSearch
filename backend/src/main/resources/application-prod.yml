spring:
  application:
    name: cardSearch

  datasource:
    url: jdbc:postgresql://postgres:5432/card_seacch_db
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop


